describe('SnakeTheCat', () => {
	it.todo('updates position of snake the cat image when hovered over', () => {
		// TODO
	});

	it.todo('sets x position to 150 when image bounds would exceed page', () => {
		// TODO
	});

	it.todo('sets y position to 150 when image bounds would exceed page', () => {
		// TODO
	});
});
